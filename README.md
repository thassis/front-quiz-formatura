# front-quiz-formatura

Simple React app that connects to an existing WebSocket server.

## Run locally

```bash
npm install
npm run dev
```

Set the server URL with `VITE_WEBSOCKET_URL` (default: `ws://localhost:8080`).
